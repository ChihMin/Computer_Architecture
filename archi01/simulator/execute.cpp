#include "simulator.h"

namespace Simulator{
	void execute(){

		/*
		while( PC / 4 >= iimage_words ){
			cycle++ ;	// CPU Cycle
			Instruction cur_ins = ins[PC];
			PC = PC + 4;	// Program Counter Point to next location
			
			switch(cur_ins.opcode){
				if 
			}
		}
		*/
	}
}
